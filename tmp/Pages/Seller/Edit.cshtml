@page "{id:int}"
@model E_Invoice_system.Pages.Seller.EditModel
@{
    ViewData["Title"] = "Edit Seller";
}

<div class="dashboard-panel">
    <div class="page-header">
        <h1 class="page-title-large">
            Edit Seller
        </h1>
        <a asp-page="./Index" class="btn-secondary">
            <i class="ph-arrow-left"></i> Back to List
        </a>
    </div>

    <component type="typeof(E_Invoice_system.Components.Seller.SellerForm)" render-mode="ServerPrerendered" param-SellerId="@Model.Seller.id" />
</div>
